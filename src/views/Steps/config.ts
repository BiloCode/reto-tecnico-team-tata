import ProtectionSelect from "./ProtectionSelect";
import Register from "./Register";

const steps_config = [
  {
    step : 0,
    Component : Register
  },
  {
    step : 1,
    Component : ProtectionSelect
  }
];

export default steps_config;